<Window x:Class="ToggleSwitchDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToggleSwitchDemo"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="200" Background="#2a2a2a">
    <Window.Resources>
        <Style x:Key="ToggleSwitchStyle"
               TargetType="CheckBox">
        </Style>
    </Window.Resources>
    <Grid HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <local:ToggleSwitch Width="45"
                            Height="25"
                            Foreground="#9b59b6" />
        <local:ToggleSwitch Width="60"
                            Height="34"
                            Foreground="Blue" Grid.Row="1"/>
        <local:ToggleSwitch Width="100"
                            Height="55"
                            Foreground="#20BF55" Grid.Row="2"/>
    </Grid>
</Window>
